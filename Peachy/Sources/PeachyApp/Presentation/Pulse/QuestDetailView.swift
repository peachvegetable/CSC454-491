// This functionality has been moved inline to PulseView.swift
// Keeping this stub file to satisfy Xcode project references

import SwiftUI

// Empty stub to prevent build errors
struct QuestDetailView: View {
    let quest: Quest
    
    var body: some View {
        EmptyView()
    }
}